/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['tailwindcss.com', 'images.pexels.com', 'randomuser.me'], // Add 'randomuser.me' here
  },
};

export default nextConfig;
